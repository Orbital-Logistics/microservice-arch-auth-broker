spring:
    application:
        name: user-service-test

    flyway:
        baseline-on-migrate: true
        placeholders:
          adminUsername: admin
          adminEmail: admin@ya.ru
          adminPassword: 1234

    cloud:
        config:
          enabled: false

eureka:
  client:
    enabled: false